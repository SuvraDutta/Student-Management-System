🧠 What this function does (very simple)

👉 “Go to database → get all students → return them”

Step-by-step:

### connect_db() → connects to MySQL
### cursor() → allows SQL execution
### SELECT * FROM students → get all rows
### fetchall() → store results
### return data